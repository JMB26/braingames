{# {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }} #}
<div class="container">
	<div class="row justify-content-center">
		<div class="fs-2 col-6 m-3 text-center">

			{{ form_start(form) }}

			<label class="fs-4 left">Etat</label>
			{{ form_widget(form) }}

			<button class="btn text-center">
				{{ button_label|default('Enregistrer') }}
			</button>

			{{ form_end(form) }}
		</div>
	</div>
</div>
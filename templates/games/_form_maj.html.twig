{# <form class="fs-4 text-center" action="{{ path('app_games_new') }}" method="POST">

	<label for="fname">Marque/fabricant</label><br>
	<input type="text" id="fname" name="fname" value=""><br>
	<label for="lname">Nom</label><br>
	<input type="text" id="lname" name="lname" value=""><br><br>
	<label for="lname">Code ISBN</label><br>
	<input type="text" id="lname" name="lname" value=""><br><br>
	<label for="lname">Age recommandé à partir de :
	</label>
	<select name="idcat" id="idcat">
		<option value="3">3</option>
		<option value="7">7</option>
		<option value="12">12</option>
		<option value="16">16</option>
		<option value="18" selected>18</option>
	</select><br><br>

	<label for="lname">Description</label><br>
	<textarea name="" id="" cols="30" rows="5"></textarea><br><br>


	<div class="d-flex fs-6 justify-content-around">
		<div>
			<label for="">Image Actuelle</label><br>
			{% if game.img is defined and game.img is not null %}
				<img src="{{ asset('images-test/' ~ game.img ) }}" alt="" style="width:100px"><br><br>
			{% else %}
				<img src="{{ asset('images-test/defaultImg.jpg') }}" alt="" style="width:100px"><br><br>
			{% endif %}
		</div>


		<div class="text-center">
			<label for="">Charger une autre image</label><br>
			<input type="file" id="imgfile" name="imgfile" value="Choose a video please" onchange="NewImgView()">			
			<p id="demo"></p>
			<img id="imgdemo" src="" alt="" style="width:100px"><br><br>
		</div>
		
	</div>

	<div class="d-flex fs-6 justify-content-center">
		<label for="lname">Type de Jeu</label><br>
		<div class="fs-5 col-6 text-center m-0">
			<select name="idcat" id="idcat">
				{% for cat in categories %}
					<option value="{{ cat.id }}">{{ cat.nom }}</option>
				{% endfor %}
			</select>
		</div>
	</div>

	<button type="submit" class="btn fs-4">{{ button_label|default('Enregistrer ce Nouveau Jeu') }}</button>
</form>

<script>
function NewImgView() {
  var x = document.getElementById("imgfile").value;
  var y = document.getElementById("imgfile").files;

  console.log(y);
  document.getElementById("demo").innerHTML = "You selected: " + x;
  var img = document.getElementById("imgdemo");  
  img.src=y;
}
</script> #}

<div class="fs-5 justify-content-start">
	{{ form_start(form) }}
	{{ form_label(form.mark, 'Marque/fabricant') }}
	{{ form_widget(form.mark) }}
	{{ form_label(form.nom, 'Nom du Jeu') }}
	{{ form_widget(form.nom) }}
	{{ form_label(form.isbn, 'Numéro ISBN') }}
	{{ form_widget(form.isbn) }}
	{{ form_label(form.idcat, 'Type de jeu') }}
	{{ form_widget(form.idcat) }}
	{{ form_label(form.txt, 'Description') }}
	{{ form_widget(form.txt) }}

	{{ form_label(form.age, 'Age, recommandé à partir de :') }}
	{{ form_widget(form.age) }}

	{{ form_row(form.img) }}
	{% if form.img.vars.data is defined and form.img.vars.data is not null %}
		<label for="">Image Actuelle</label>
		<div><img src="{{ asset('images-test/' ~ game.img ) }}" alt="" style="width:100px"></div>
		<div class="fs-6">
			{{form.img.vars.data}}
		</div>
	{% else %}
		<label for="">Image Actuelle par Défaut</label>
		<div><img src="{{ asset('images-test/defaultImg.jpg') }}" alt="" style="width:100px"></div>
		<div class="fs-6">
			{{form.img.vars.data}}
		</div>
	{% endif %}

    {{ form_label(form.note, 'Notation') }}
    {{ form_widget(form.note) }}

    {{ form_label(form.status, 'Status de Publication') }}
    {{ form_widget(form.status) }}
</div>


<div class="text-center">
	<button class="btn text-center fs-3">
		{{ button_label|default('Enregistrer') }}
	</button>
</div>
{{ form_end(form) }}

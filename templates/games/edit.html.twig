{% extends 'base.html.twig' %}

{% block title %}Edit Games
{% endblock %}

{% block body %}
	<div class="container">
		<div class="row justify-content-center">
			<div class="fs-2 col-6 text-center m-3">
				Gestion de la Mise à Jour d'un Produit...
			</div>
		</div>

		<div class="row justify-content-center">
			<div class="fs-4 col-12 text-center m-3">
				{{ include('games/_form.html.twig', {'button_label': 'ENREGISTRER'}) }}
				{{ include('games/_delete_form.html.twig') }}
				<a href="{{ path('app_games_index') }}">Retour à la liste</a>
			</div>
		</div>
	</div>
{% endblock %}

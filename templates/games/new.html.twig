{% extends 'base.html.twig' %}

{% block title %}New Games{% endblock %}

{% block body %}
    {# <h1>Create new Games</h1>

    {{ include('games/_form.html.twig') }}

    <a href="{{ path('app_games_index') }}">back to list</a> #}


<div class="container">
		<div class="row justify-content-center">
			<div class="fs-2 col-6 text-center m-3">
				Création d'un Nouveau Produit...
			</div>
		</div>

		{{ include('games/_form.html.twig') }}

		<div class="fs-4 text-center m-3">
			<a href="{{ path('app_games_index') }}">Retour à la Liste des Produits</a>
		</div>
	</div>

{% endblock %}

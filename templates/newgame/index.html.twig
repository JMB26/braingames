{% extends 'base.html.twig' %}

{% block title %}Hello NewgameController!
{% endblock %}

{% block body %}

	<div class="container">
		<div class="row justify-content-center">
			<div class="fs-2 col-6 text-center m-3">
				Ajouter ce jeu à vos echanges...
			</div>
		</div>

		<div class="row justify-content-center">
			<div class="fs-4 col-12 text-center m-3">

				<table class="table fs-5">
					<thead>
						<tr class="text-center">
							<th scope="col" class="fdtitre"></th>
							<th scope="col" class="fdtitre">Marque</th>
							<th scope="col" class="fdtitre">Modèle</th>
							<th scope="col" class="fdtitre">Catégorie</th>
							<th scope="col" class="fdtitre text-center">Descriptif</th>
							<th scope="col" class="fdtitre">Etat</th>
							<th scope="col" class="fdtitre">Age</th>
						</tr>
					</thead>
					<tbody>


						<tr>
							<td>
								<a class="nav-link fs-6 text-black-50" title="Proposer le votre ?" href="{{ path('app_newgame', {'id': game.id}) }}">
									{% if game.img is defined and game.img is not null %}

										<img src="{{ asset('images-test/' ~ game.img ) }}" alt="" style="width:100px">
									{% else %}
										<img src="{{ asset('images-test/defaultImg.jpg') }}" alt="" style="width:100px">
									{% endif %}
								</a>
							</td>

							<td>{{ game.mark }}</td>
							<td>{{ game.nom }}</td>
							<td>{{game.idcat.nom}}</td>

							<td>{{ game.txt|u.truncate(15) }}...</td>

							<td>Etat...</td>
							<td>{{ game.age }}</td>

						</tr>

					</tbody>
				</table>

				<div class="row justify-content-center">
					<div class="fs-2 col-6 text-center m-3">
						Précisez l'Etat...
					</div>
					<div class="fs-5 col-6 text-center m-3">
						<select name="shape" id="shape">
							{% for shape in shapes %}
								<option value="{{ shape.id }}">{{ shape.etat }}</option>
							{% endfor %}
						</select>
					</div>
				</div>


				<a href="{{ path('app_games_new') }}">Enregistrer ce Nouvel Echange</a>

			</div>
		</div>
	</div>


	{# <div class="container">
					<div class="row justify-content-center">
						<div class="fs-3 col-6 text-center m-4">
							Ajouter un nouveau jeu à vos echanges...
						</div>
					</div>
				</div>	
			
					<div class="row justify-content-center">
						<div class="fs-4 col-12 text-center m-3">
			
							<table class="table fs-5">
								<thead>
									<tr class="text-center">
										<th>#</th>
										<th>Marque</th>
										<th>Nom</th>
										<th>Isbn</th>
										<th>Age</th>
										<th>Photo</th>
										<th>Description</th>
										<th>Note</th>
										<th>Status</th>
										<th>Date</th>
										<th>actions</th>
									</tr>
								</thead>
								<tbody>
								
										<tr>								
											<td>{{ id }}</td> #}


{# <td>{{ game.mark }}</td>
								<td>{{ game.nom }}</td>
								<td>{{ game.isbn }}</td>
								<td>{{ game.age }}</td>
								<td>
									{% if game.img is defined and game.img is not null %}
										<img src="{{ asset('images-test/' ~ game.img ) }}" alt="" style="width:100px">
									{% else %}
										<img src="{{ asset('images-test/defaultImg.jpg') }}" alt="" style="width:100px">
									{% endif %}

								</td>
								<td>{{ game.txt|u.truncate(15) }}...</td>
								<td>{{ game.note }}</td>
								<td>{{ game.status ? 'Yes' : 'No' }}</td>
								<td>{{ game.date ? game.date|date('d-m-Y') : '' }}</td>
								<td>
									<a href="{{ path('app_games_edit', {'id': game.id}) }}">Modifier</a>
									<a href="{{ path('app_games_show', {'id': game.id}) }}">Supprimer</a>
								</td> #}
	{# </tr>
									
								</tbody>
							</table>
			
							<a href="{{ path('app_games_new') }}">Enregistrer un nouveau Jeu</a>
			
						</div>
					</div>
				</div> #}

{% endblock %}

{{ form_start(form) }}

<div class="text-start"></div>

<div class="text-start visually-hidden">
	{{ form_row(form.swapuser) }}
	{{ form_row(form.iduser, { 'attr': {'class': 'hidden'}}) }}
	{{ form_row(form.swapbuyer, { 'attr': {'class': 'hidden'}}) }}
	{{ form_row(form.idbuyer, { 'attr': {'class': 'hidden'}}) }}
	{{ form_row(form.idgameuser, { 'attr': {'class': 'hidden'}}) }}
	{{ form_row(form.Idswapbuyer, { 'attr': {'class': 'hidden'}}) }}
	{{ form_row(form.idshape, { 'attr': {'class': 'hidden'}}) }}
</div>

<div class="row justify-content-center">
	<div class="fs-2 col-6 text-center m-3">
		Précisez l'Etat...
	</div>
	<div class="fs-5 col-6 text-center m-0">
		<select name="idshape" id="shape">
			{% for shape in shapes %}
				<option value="{{ shape.id }}">{{ shape.etat }}</option>
			{% endfor %}
		</select>
	</div>

	<button class="btn fs-4">{{ button_label|default('Enregistrer ce Nouvel Echange') }}</button>
	{{ form_end(form) }}

<div class="row justify-content-center">
	<div class="fs-2 col-6 text-center m-3">
		Précisez l'Etat...
	</div>
	<div class="fs-5 col-6 text-center m-0">
		<form class="text-center" action="{{ path('app_swap_new', {'id': id}) }}" method="POST">
			<select name="idshape" id="shape">
				{% for shape in shapes %}
					<option value="{{ shape.id }}">{{ shape.etat }}</option>
				{% endfor %}
			</select>
		</div>
	</div>
	<button type="submit" class="btn fs-4">{{ button_label|default('Enregistrer ce Nouvel Echange') }}</button>
</form>
